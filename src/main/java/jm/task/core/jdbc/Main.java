package jm.task.core.jdbc;

import jm.task.core.jdbc.dao.UserDaoJDBCImpl;
import jm.task.core.jdbc.service.UserService;
import jm.task.core.jdbc.service.UserServiceImpl;

public class Main {
    public static void main(String[] args) {
        // реализуйте алгоритм здес
        /*
        UserService userService = new UserServiceImpl();
        userService.createUsersTable();

        userService.saveUser("Anna", "Bork", (byte) 24);
        userService.saveUser("Andrey", "Smit", (byte) 33);
        userService.saveUser("Ben", "Mask", (byte) 18);
        userService.saveUser("Fedor", "Valiev", (byte) 56);
        userService.saveUser("Roman", "Abramov", (byte) 44);

        userService.getAllUsers();
        userService.cleanUsersTable();
        userService.dropUsersTable();

         */
    }
}
